#include "ft_printf.h"

#include <stdio.h>

int main()
{
	int a = 10;
	void *p;
	p = &a;
	/*if ((long)p > 10)
	{
		printf("%p hola \"original\"\n", p);
	}*/
	//ft_printf("%s %c %i %x %X ", "hola", 'd', 10, 20, 10);
	ft_printf("%s", "");
	   printf("%s", "");
	return (0);
}